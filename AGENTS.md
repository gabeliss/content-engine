# Agent Instructions

- This React app uses Tailwind utilities for UI styling. Prefer Tailwind classes in components for new layout and presentation work.
- Do not add new standalone CSS files for feature-specific UI unless the user explicitly asks for stylesheet changes.
- Reuse existing shared components and styling primitives before introducing new surface area.
